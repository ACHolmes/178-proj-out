import gatherDefaultMapData from "./gatherDefaultMapData";
import route_data from "./data/data_by_route.json";

const gatherResultsMapData = (liveBusData, fastestRoutes, setRoutes, setStops, setBuses) => {

  console.log(fastestRoutes);
  return;

};

export default gatherResultsMapData;